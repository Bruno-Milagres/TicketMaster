# === TicketMaster — Variáveis de Ambiente ===
# Copie este arquivo para .env e preencha os valores.
# O arquivo .env NÃO deve ser commitado (está no .gitignore).

# Senha do SA do SQL Server (deve atender aos requisitos de complexidade do SQL Server)
MSSQL_SA_PASSWORD=TicketMasterDev2026!

# String de conexão (alternativa — use dotnet user-secrets para desenvolvimento)
# ConnectionStrings__DefaultConnection=Server=localhost,1433;Database=TicketMasterV2;User Id=sa;Password=YOUR_PASSWORD;TrustServerCertificate=True;
